node_modules
out
.cache
.DS_Store
*.log
.env
.env.local

# Generated artifacts (regenerated by `npm run prepare:all`)
public/extracted

# src/data/scenes.json is committed as a fallback so Remotion can compile
# without first running the pipeline. The pipeline overwrites it in CI.
