node_modules
out
.cache
.DS_Store
*.log
.env
.env.local

# Generated artifacts (regenerated by `npm run prepare:all`)
public/extracted

# public/audio/master-voiceover.mp3 is a Stage-1 copy of input/master-voiceover.mp3.
# Keep the canonical file in input/, regenerate the public/ copy in CI.
public/audio/*.mp3

# Stage 1 outputs (regenerated by extract + transcribe).
src/data/pptx-extracted.json
src/data/master-transcript.json
src/data/master-transcript.txt

# Stage 2 outputs (authored by Claude based on Stage 1 outputs).
# Committed by hand; not generated, not gitignored.

# src/data/scenes.json is committed as a fallback so Remotion can compile
# without first running the pipeline. The pipeline overwrites it in CI.
